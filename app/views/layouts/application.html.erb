<!DOCTYPE html>
<html>
<head>
  <title>
  	<% if @title.blank?%>	<!-- HTML Title -->
  		MEbGDP
  	<% else %>
  		MEbGDP | <%= @title %>
  	<% end %>
  </title>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= render 'layouts/script' %>	<!-- Necessary scripts -->
  <%= render 'layouts/stylesheet' %> <!-- Necessary styles -->
  <%= csrf_meta_tags %>
</head>
<body>
 <header>
 	<%= render 'layouts/header' %>
 </header>
	
 <section>
 	 <div class="major">
  	 	<%= yield %>
  	 </div>
 </section>
   
  <footer>
  	<%= render 'layouts/footer' %>
  </footer>

</body>
</html>
